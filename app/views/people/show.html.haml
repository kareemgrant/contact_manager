%p#notice= notice
%p
  %b First name:
  = @person.first_name
%p
  %b Last name:
  = @person.last_name
%p
  %b Phone numbers:
  = print_numbers(@person.phone_numbers)
  = link_to 'Add a phone number', new_phone_number_path(person_id: @person.id)
= link_to 'Edit', edit_person_path(@person)
|
\#{link_to 'Back', people_path}
